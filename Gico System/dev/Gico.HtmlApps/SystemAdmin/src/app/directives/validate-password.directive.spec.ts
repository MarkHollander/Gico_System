import { ValidatePasswordlDirective } from './validate-password.directive';

describe('ValidateEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePasswordlDirective();
    expect(directive).toBeTruthy();
  });
});
